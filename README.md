# discount-management-system
Discount Management System built with .NET Web API and React (TypeScript). Supports multiple discount types using OOP, polymorphism, and Factory pattern. Includes CRUD operations, discount simulation, Swagger documentation, and EF Core InMemory.
